import * as React from 'react'
import Link from 'gatsby-link'

const PageRowComponent = (props: any) => {
    return (
        < div >
            {props.children}
        </div >
    )
}

export default PageRowComponent
